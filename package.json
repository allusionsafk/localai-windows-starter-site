{
  "name": "localai-windows-starter-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AFK-LocalAI website: landing + docs for localai-windows-starter. Static assets + a Cloudflare Worker for live release info.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node tests/test-worker.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
