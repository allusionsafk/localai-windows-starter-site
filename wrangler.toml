# Optional — only needed for `wrangler pages dev/deploy` from the CLI.
# The Cloudflare dashboard Git integration does not require this file.
name = "localai-windows-starter"
pages_build_output_dir = "public"
compatibility_date = "2024-11-01"
